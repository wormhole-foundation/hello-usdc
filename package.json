{
  "name": "hello-usdc",
  "version": "1.0.0",
  "main": "ts-scripts/deploy.ts",
  "repository": "git@github.com:wormhole-foundation/hello-usdc.git",
  "author": "Wormhole Contributors",
  "license": "Apache 2.0",
  "scripts": {
    "build": "forge build",
    "deploy": "npm run build && ts-node ./ts-scripts/deploy.ts",
    "test": "forge test",
    "integration-test": "npm run build && ts-node ./ts-scripts/integration-test.ts",
    "clean": "rm -rf out/"
  },
  "dependencies": {
    "ethers": "^6.14.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
